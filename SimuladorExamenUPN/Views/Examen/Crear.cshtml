

<h2>Crear Examen</h2>
<span id="CrearExamenLink"></span>
<form action="/Examen/Crear" method="post">

    <div class="form-group">
        <label>Tema</label>
        <select id="TemaSelectExamenCrearLink" class="form-control" name="TemaId">
            <option value="">-- Seleccione una opcion --</option>
            @foreach(var item in ViewBag.Temas)
            {
                <option value="@item.Id">@item.Nombre</option>
            }
        </select>
        @Html.ValidationMessage("TemaId")
    </div>

    <div class="form-group">
        <label>Nro de Preguntas</label>
        <input type="text" id="PreguntasCrearExamenLink" class="form-control" name="nroPreguntas"  />
        @Html.ValidationMessage("nroPreguntas")
    </div>
   

    <button class="btn btn-primary" id="GuardarCrearExamenLink">Guardar</button>
    <a class="btn btn-link" id="CancelarCrearExamenLink" href="/Examen/Index">Cancelar</a>
</form>
